qutip>=5.0
numpy>=1.21
matplotlib>=3.5
statsmodels>=0.13
scipy>=1.8
pymc>=5.0
arviz>=0.12
rich>=12.0
streamlit>=1.20
pytest>=7.0
pytest-cov>=4.0
# Remove jax[cuda] and pytensor as they cause compilation issues
# jax[cuda]
# pytensor
# threading is built-in, no need to specify
